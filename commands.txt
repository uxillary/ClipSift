
cd C:\Users\trigg\Documents\GitHub\clipsift
py -0p
python --version
nvidia-smi
py -3.11 --version
py -3.11 -m venv .venv

.\.venv\Scripts\Activate.ps1

If PowerShell blocks activation, allow it for this window only:

Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1

python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt

Verify PyTorch and CUDA

python -m pip install --upgrade pip
python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

Run:

python -c "import torch; print('PyTorch:', torch.__version__); print('CUDA available:', torch.cuda.is_available()); print('PyTorch CUDA:', torch.version.cuda); print('Device:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"

may vary for 3070ti

python -m pip install -r requirements.txt
python -m pip install bitsandbytes

verify

python -c "import torch, transformers, accelerate, bitsandbytes, cv2, PIL; print('Torch:', torch.__version__); print('Transformers:', transformers.__version__); print('Accelerate:', accelerate.__version__); print('BitsAndBytes:', bitsandbytes.__version__); print('CUDA:', torch.cuda.is_available()); print('GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"

python -m pytest

or 

$env:PYTHONPATH = (Get-Location).Path
python -m pytest

auth
hf auth login
hf auth whoami

access?

python -c "from huggingface_hub import model_info; info=model_info('google/gemma-3-4b-it'); print('Access confirmed:', info.id)"

test 

python -m clipsift.cli test-image local_test_data\person.png --device auto --preset safe